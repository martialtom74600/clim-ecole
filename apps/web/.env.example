# Clim École — Next.js (apps/web)

# Supabase — source de données prod (prioritaire si défini)
SUPABASE_URL=https://ktraibazjqwqfrnwrqyq.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# CSV fallback local (si Supabase absent)
# CLIM_CSV_PATH=data/output_prospection.csv

# Auth (production obligatoire)
AUTH_SECRET=change-me-long-random-string
ADMIN_PASSWORD=your-admin-password

# Stripe
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRICE_DOSSIER=price_...
STRIPE_PRICE_PRO=price_...
NEXT_PUBLIC_APP_URL=http://localhost:3001

# Exclusivité — max achats unitaires par territoire (défaut: 3)
PACK_MAX_UNLOCKS=3

# Email transactionnel (Resend) — post-achat + alertes
RESEND_API_KEY=re_...
EMAIL_FROM=Clim École <radar@strate.studio>

# Analytics (optionnel)
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=clim-ecole.fr

# Dev unlock API (optional, local testing without Stripe)
DEV_UNLOCK_KEY=dev-unlock
